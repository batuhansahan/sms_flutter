import 'package:flutter/material.dart';

const appbarColor = Colors.indigo;

const feedbackButtonColor = Colors.indigo;
const messageButtonColor = Colors.blue;
const contactButtonColor = Colors.green;
const checkboxActiveColor = Colors.green;

const floatingButtonLabelTextStyle = TextStyle(fontSize: 14,fontWeight: FontWeight.w500);

const messageTextareaColor = Colors.white;

const messageButtonIcon = Icon(Icons.message);
const feedbackButtonIcon = Icon(Icons.home);
const contactButtonIcon = Icon(Icons.create);
